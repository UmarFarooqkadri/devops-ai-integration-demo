apiVersion: v1
kind: Service
metadata:
  name: devops-ai-platform
  namespace: devops-ai
  labels:
    app: devops-ai-platform
spec:
  type: ClusterIP
  selector:
    app: devops-ai-platform
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
